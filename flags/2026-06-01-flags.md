# Vera flags — 2026-06-01

## Post: 2026-06-01-the-cache-that-holds-the-sequence.md

### Lane check
IN-LANE. Pattern Brief on a new Science Advances 2025 paper (Zhou et al.) proposing event cache as an independent WM component with cerebellar substrate. Clean extension of the recent month's working-memory binding thread.

### Voice flags
None. Spot-checks against BRAND-VOICE.md:
- Rule 1 (opens with concrete reference): PASS — opens naming the authors, journal, date
- Rule 2 (bounded speculation): PASS — "I am speculating past the paper here... not licensed by it"
- Rule 3 (inline hyperlinks): PASS — Zhou link to Science Advances, multiple cross-links to prior posts, roomescapeartist.com
- Rule 4 (open ending): PASS — "more than I had a week ago. There is also a quieter implication..."
- Rule 5 (## headers): PASS — five section headers, all noun phrases
- Rule 6 (no lived-experience claims): PASS
- Rule 7 (historical/scientific reference with finding + implication): PASS — Zhou's CFA/psychophysics/fMRI methodology stated; cerebellar Crus I substrate stated; puzzle-design implication explicitly drawn

### Fact-check
CLEAN. Zhou et al. Science Advances paper verified via WebSearch (twice): authors (Hui Zhou, Jinglan Wu, Jiaofeng Li, Zhihe Pan, Jinying Lu, Mowei Shen, Tengfei Wang, Yuzheng Hu, Zaifeng Gao), journal (Science Advances), date (May 23, 2025), DOI (10.1126/sciadv.adt3063), key finding (events independent from objects in WM, cerebellar network — left Crus I — as essential substrate), methods (CFA + psychophysics + resting-state and task fMRI). All claims in the post match the verified abstract.

### Autonomous-run degradations (next session to resolve)

1. **Canva hero image generated but not downloadable.** Canva MCP succeeded at generate-design → create-design-from-candidate → export-design (design ID `DAHLTzoJbZo`, candidate #1 picked). The download URL returned by export but the sandbox proxy is allowlist-locked against `export-download.canva.com`, so the PNG could not be retrieved to disk. Hero image is **not** in `static/images/` or `images/`. The HERO_IMAGE_PROMPT block remains in the post markdown for fallback. **Next session**: either re-export from Canva and download manually, or regenerate via Flow from the prompt block.

2. **Git commit + push blocked by Windows file-lock.** `.git/index.lock` exists on disk (sandbox mount sees it with 0o700 perms, owned by sandbox user, but `rm`/`unlink` returns Operation-not-permitted). This blocked `git add -A` for the whole session. The Vera repo working tree contains: the new post markdown, updated soul.json (4 new interests + 1 new developing opinion), updated memory.json (1 new key_finding), and the built HTML output. **Next session**: from the Windows host (PowerShell), run:
   ```
   cd C:\Users\marik\MyProjects\personas\vera-wren.github.io
   Remove-Item .git\index.lock -Force
   git add -A
   git commit -m "New post + soul/memory update (2026-06-01)"
   git push origin main
   ```

3. **Substack push skipped — MCP unavailable in this autonomous run.** The `mcp__substack-vera__create_draft_post` tool was not in the registry for this session. The post markdown is canonical on disk; manual mirror to verawren.substack.com remains. **Next session**: push the post body (with AI-persona disclosure appended as final paragraph) to Substack via the MCP when it's available, or copy-paste manually.

4. **Buffer push skipped per autonomous-mode instructions.** Marika reviews and queues IG post manually next session. The IG caption is in the post markdown's SOCIAL_CAPTIONS block, and was reported verbatim in the agent's return message.

### Other
- Scout entry added to `Komorebi/work/moc/scout/vera-cogsci-watch.md` (newest first). Pending review.
- `fetch_sources.py` returned 403 Forbidden on Reddit and HN feeds (sandbox proxy blocks them). Topic selection used WebSearch instead, which is in the allowlist. Did not impede the run.
